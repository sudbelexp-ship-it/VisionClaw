*.xcframework/
# Vendored on purpose: official ggml-org/whisper.cpp release asset, checksum verified by hand
# before committing (see AudioCaptureHub.swift's header for why it's here instead of via SPM).
!Vendor/whisper.xcframework/
!Vendor/whisper.xcframework/**
.DS_Store
xcuserdata/

# FastVLM model files (large binaries, download separately)
CameraAccess/FastVLM/models/

# Local secrets (API keys, tokens)
CameraAccess/Secrets.swift

# Xcode workspace metadata
*.xcworkspace/

# Node server
server/node_modules/
server/package-lock.json
